[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
startCommand = "sh -c 'uvicorn src.main:app --host 0.0.0.0 --port ${PORT:-8080} --workers 1'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
